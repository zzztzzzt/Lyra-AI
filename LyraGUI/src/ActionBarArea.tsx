import React from "react";

interface Props {
  gradient: string;
}

const ActionBarArea: React.FC<Props> = ({ gradient }) => {
  return(
      <div className="">
        
      </div>
  );
};

export default ActionBarArea;